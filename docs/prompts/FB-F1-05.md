# FB-F1-05 — Remediación 3 Fase 1: hallazgos de FB-F1-AUD-03

- **ID:** FB-F1-05
- **Fecha:** 19 de junio de 2026
- **Destino:** Claude Code
- **Propósito:** Corregir los 3 hallazgos residuales de FB-F1-AUD-03, con barridos completos para converger y no repetir ronda.

## Contexto
FB-F1-AUD-03 devolvió 3 hallazgos chicos: la migración sin el RLS enable explícito, una expectativa de test vieja que quedó en otro archivo, y un aria-label hardcodeado. Vamos convergiendo. Este prompt los corrige y barre completo las dos clases de problema que ya se repitieron.

## Hallazgos implementados

### (03-01) Enable RLS explícito en la migración

`supabase/migrations/0004_rls_fixes.sql`: Agregado `ALTER TABLE storage.objects ENABLE ROW LEVEL SECURITY;` al inicio del archivo, antes de los DROP POLICY. En Supabase real ya viene habilitado; lo dejamos explícito para alinear migración y mock (`setup.sql`) y cumplir el DoD.

### (03-02) Alineación de tests con 0004 — barrido completo

**`tests/integration/mi-perfil.test.ts` línea 294:**
- Test renombrado: `'supervisor NO ve documentos de su equipo (SELECT, solo propio → 0 filas)'`
- Aserción `n1` corregida: `toBeGreaterThanOrEqual(1)` → `toBe(0)`
- Aserción `n2` ya era correcta (`toBe(0)`), solo se actualizó el comentario

**`tests/integration/mi-perfil.test.ts` línea 478:**
- Test renombrado: `'supervisor NO ve objetos de carpetas ajenas — solo la propia (0004_rls_fixes)'`
- Aserción ya era correcta (`rows.toHaveLength(0)`) — el nombre era engañoso ("y la de su equipo")

**Barrido completo:** Revisados todos los archivos de integración. Solo `rls.test.ts` y `mi-perfil.test.ts` tenían tests sobre documentos del supervisor. El de `rls.test.ts` ya fue corregido en FB-F1-04. Los `toBeGreaterThanOrEqual(1)` restantes corresponden a acciones de admin (UPDATE profiles, aprobar/rechazar documentos) — son correctos.

### (03-03) Copy sweep completo — accesibilidad y atributos

**`lib/copy/index.ts`:** Agregadas 2 nuevas claves:
- `general.logoAlt: 'Logo First Blades'` — para `alt` de logos
- `topbar.menuButton: 'Menú'` — para `aria-label` del botón hamburguesa

**Cambios en componentes:**
- `components/layout/Topbar.tsx`: `aria-label="Menú"` → `aria-label={copy.topbar.menuButton}`
- `app/login/page.tsx`: ambas instancias de `alt="First Blades"` → `alt={copy.general.logoAlt}`
- `components/layout/Sidebar.tsx`: `alt="First Blades"` → `alt={copy.general.logoAlt}`

**Barrido completo:** Revisados todos los archivos `.tsx` en `app/` y `components/`. `LoginForm.tsx` ya usaba `copy.auth.login.showPassword/hidePassword` para su aria-label. Resultado: cero strings hardcodeados en atributos de accesibilidad o imagen.

## Fuera de alcance

- Mail de rechazo (diferido §14).
- Módulo Equipo completo.
- RLS de `profiles` del supervisor (correcto según Constitución §6).
- No se tocó `storage_path`.
