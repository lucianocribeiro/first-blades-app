# FB-F3-02 — Fix de hallazgos FB-F3-AUD-01 (cimiento del Calendario, migración 0009)

> **Fase:** 3 — Calendario de Rotaciones
> **Tipo:** Build / Fix (cierra los hallazgos de la auditoría FB-F3-AUD-01)
> **Sobre:** la branch de FB-F3-01 (`feat/fb-f3-01-calendario-rotaciones-cimiento`, PR #3)
> **Constitución vigente:** v0.5 (`docs/constitucion.md`) · **PRD:** `docs/prd-fase-3.md`

---

## Objetivo

Dejar el cimiento del calendario **apto para `db push`** cerrando los hallazgos de `docs/audits/FB-F3-AUD-01.md`. No cambia el alcance funcional: reconcilia el historial de migraciones, endurece los tests de RLS y el drift detector, y ajusta la RLS de `rotation_groups`. El esquema **sigue sin ir a producción** en esta tarea.

**Contexto de la auditoría:** veredicto "requiere fix previo al push". `0009` confirmada como aditiva y no destructiva, y NO aplicada a remoto. Los bloqueantes son historial de migraciones, cobertura de tests RLS y estrictez del drift detector.

---

## Orden de trabajo

### Paso 0 — Historial de migraciones (DIAGNÓSTICO con reporte previo, antes de tocar nada)

La auditoría detectó que el remoto tiene `0008` (es `notification_log` de Fase 2) pero la branch local no lo incluye (`find` lista `0001`–`0007` + `0009`, sin `0008`); y `0009` es local-only.

1. Diagnosticá sin modificar nada todavía:
   - `git fetch --all --prune`
   - Verificá si `main` contiene `supabase/migrations/0008_*` (`git ls-tree -r --name-only origin/main -- supabase/migrations`).
   - `git log --oneline origin/main -- supabase/migrations` y comparalo con la branch actual.
   - Determiná si la branch simplemente **está atrasada respecto de `main`** (esto es lo esperado: `0008` se mergeó a `main` en Fase 2 y esta branch se cortó antes).
2. **REPORTÁ el diagnóstico** (qué contiene `main`, qué le falta a la branch, causa raíz) antes de actuar.
3. Resolución según el diagnóstico:
   - **Si la branch está atrasada respecto de `main` (que sí tiene `0008`):** traé `main` a la branch (`git  deje el árbol más limpio), incorporando el archivo `0008` sin renumerar ni reescribir migraciones ya aplicadas. Confirmá que `0009` sigue siendo la última y no colisiona.
   - **Si `0008` NO existe en `main` ni en ninguna parte del repo (nunca se commiteó):** **FRENÁ y escalá a Luciano.** No reescribas el historial, no fuerces, no borres. Es un problema de gobernanza que se decide con él.
4. Tras reconciliar, corré `supabase migration list` y confirmá: `0001`–`0008` con **Local = Remote**, y `0009` como **Local-only**. Reportá la salida.
5. **No ejecutes `supabase db push`.**

> Restricción dura: ninguna reescritura de historial de migraciones ni operación destructiva de git sin reportar primero y sin OK de Luciano.

### Paso 1 — Tests RLS de escritura para `rotation_*` (Hallazgo 4)

En `tests/integration/rls.test.ts`, para **`rotation_groups`** y **`rotation_assignments`**, agregá casos que faltan, con el patrón real ya usado (bajo `asUser(...)`, JWT del rol acotado, operando por id directo sin filtro de app):

- **Empleado:** `UPDATE` denegado y `DELETE` denegado.
- **Supervisor:** `UPDATE` denegado y `DELETE` denegado (tanto sobre filas propias como de su equipo).
- **Admin:** al menos un `UPDATE` y un `DELETE` exitosos (prueba de acceso completo).

Mantené el criterio: el "0 filas afectadas / error de policy" debe provenir de la RLS, no de un filtro de aplicación.

### Paso 2 — Endurecer el drift detector (Hallazgo 5)

En `tests/integration/migration.test.ts`, reemplazá las verificaciones laxas por validaciones exactas contra el catálogo de Postgres:

- **UNIQUE de `rotation_assignments`:** consultá `pg_constraint` + `pg_get_constraintdef(oid)` y afirmá exactamente `UNIQUE (user_id, fecha)` (columnas y orden), no "existe alguna constraint".
- **CHECK de motivo obligatorio:** afirmá la **expresión** del CHECK (que exige `motivo_ausencia IS NOT NULL` cuando `estado_dia = 'periodo_fuera_trabajo'`), no solo su nombre/tipo.
- **`profiles.dni`:** vía `information_schema.columns`, afirmá tipo `text` y `is_nullable = YES`, además de su constraint `UNIQUE`.

El detector debe quedar estricto (falla si el remoto diverge de lo esperado).

### Paso 3 — Restringir RLS de `rotation_groups` a admin-only (Hallazgo 3)

> Decisión de Luciano: `rotation_groups` pasa a **admin-only** en Fase 3 (tabla inerte, sin UI ni consumidor; `group_id` nullable). *(Si Luciano prefiere documentar la excepción en lugar de restringir, este paso se reemplaza por esa nota; construir solo tras su confirmación.)*

Como `0009` **aún no está aplicada a prod**, incorporá el ajuste **dentro de la migración `0009`** (no editar `0001_init`, que ya está aplicada; no crear una migración extra si podés folizarlo en `0009` antes del push):

- `DROP POLICY` de la policy amplia de `rotation_groups` (`rotation_groups_select_all`, `USING (auth.uid() IS NOT NULL)`).
- Dejá solo acceso completo para admin (`FOR ALL` admin) y **sin** `SELECT` para no-admin.
- Corregí el comentario de `0009` que afirma que "la RLS coincide exactamente" para que sea preciso (aclarar que ajusta `rotation_groups`).
- Agregá un test RLS: un no-admin (empleado/supervisor) **no** puede `SELECT` de `rotation_groups`.

### Paso 4 — Preflight de `dni` (Hallazgo 2 — NO es código; queda para el runbook del push)

No hay cambio de código acá. Anotá en el PR que, antes del `db push` de `0009`, hay que correr en remoto un chequeo de **DNIs no nulos duplicados** en `profiles` (la constraint `UNIQUE` fallaría si existen). Lo ejecuta Luciano con guía en el runbook de push.

---

## Datos / RLS / Seguridad

- Sin cambios de alcance funcional. La única variación de RLS es **más restrictiva** (`rotation_groups` admin-only).
- Sin secretos. Sin `createAdminClient()` en código de feature.
- Errores visibles; ningún test que degrade a `[]` ni trague excepciones.

## Skills a reutilizar

`supabase-migration` (ajuste de policy en `0009`), `dod-checklist`, y el harness `tests/integration/helpers.ts` (`asUser`, clientes por rol).

## Tests

- Nuevos tests RLS de `UPDATE`/`DELETE` denegado (empleado, supervisor) y `UPDATE`/`DELETE` admin en `rotation_*`.
- Test RLS de `rotation_groups` no legible por no-admin.
- Drift detector endurecido (UNIQUE exacto, expresión del CHECK, tipo/nullability de `dni`).
- Todo verde en CI.

## Ruta y compuertas

- Ruta: `commit → push → GitHub Actions`. CI es la compuerta. Hard-fail si los tests de integración no pueden correr contra Postgres (no skip silencioso).
- **NO ejecutar `supabase db push`.** Tras CI verde, esto vuelve a **re-auditoría de Codex (FB-F3-AUD-02)** antes del push.
- Guardá este prompt en `docs/prompts/FB-F3-02.md`.

## Definición de Done

- [ ] Diagnóstico de historial reportado; `0008` reconciliado en la branch (o escalado si no existe). `supabase migration list`: `0001`–`0008` Local = Remote, `0009` Local-only.
- [ ] Tests RLS de `UPDATE`/`DELETE` denegado para empleado y supervisor + escritura admin, en `rotation_groups` y `rotation_assignments`, pasando.
- [ ] `migration.test.ts` valida exactamente `UNIQUE (user_id, fecha)`, la expresión del `CHECK` y `profiles.dni text NULL`.
- [ ] `rotation_groups` restringida a admin-only en `0009` + comentario corregido + test de no-lectura para no-admin.
- [ ] Nota de preflight de `dni` en el PR.
- [ ] `typecheck` / `lint` / `build` / tests verdes en CI.
- [ ] **SIN `db push`.** Listo para FB-F3-AUD-02.
- [ ] Prompt guardado en `docs/prompts/FB-F3-02.md`.
