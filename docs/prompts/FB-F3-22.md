# FB-F3-22 — Fix: falla de carga del saldo en Aprobaciones debe ser visible, no tragada

> **Tipo:** fix de manejo de error (lógica de superficie, no solo-tests). Cierra el hallazgo Medio de FB-F3-AUD-21. Va sobre la branch del PR #16. Requiere **re-auditoría acotada**.
> **Sin migración.**

---

## Objetivo

Cerrar el hallazgo Medio: en `/aprobaciones`, si falla la query de `rotation_assignments` para el saldo, la página hoy hace `console.error` y sigue con `saldoByUser = {}`, de modo que el badge **no aparece** y esa ausencia se lee como dato válido ("el empleado tiene días"). Eso oculta una falla de query/RLS y puede llevar al admin a aprobar sobre un supuesto falso.

## Alcance — `app/(app)/aprobaciones/page.tsx` (~línea 61)

- Tratar `saldoError` como las otras superficies: **no** degradar a `{}` silenciosamente. Cuando la carga del saldo falla, mostrar un **estado de error visible** en el lugar del badge (o un aviso a nivel de la cola) que distinga claramente **"no se pudo calcular el saldo"** de **"tiene días disponibles"**. Reusar `copy.errors.generic` o un copy específico tipo "No se pudo calcular el saldo".
- **Mantener el resto del comportamiento:** el badge, cuando **sí** carga, sigue siendo informativo y **no bloquea** la aprobación. La falla de saldo tampoco debe romper la cola en sí (la lista de solicitudes pendientes debe seguir visible y accionable); lo que cambia es que la **ausencia de dato de saldo se comunica como error**, no como "sin badge".
- No tragar el error: nada de `catch` que devuelva `{}` sin señal visible.

## Tests

- **Actualizar** el test que hoy fija "el badge simplemente no aparece" (`tests/unit/aprobaciones-page-saldo.test.ts:117`): ahora, ante `saldoError`, debe renderizarse el **estado de error visible** del saldo (no ausencia silenciosa).
- Regresión: con saldo cargado OK, el badge aparece, es informativo y no bloquea; la aprobación/rechazo siguen funcionando.
- La cola de pendientes sigue visible aunque falle el saldo.
- Copy sin terminología técnica. Integración **hard-fail** si no puede correr.

## Skills a reutilizar

- **`dod-checklist`** para el cierre.

## Definition of Done

- La falla de carga del saldo en Aprobaciones se muestra como error visible; la ausencia de badge ya no se presenta como dato válido.
- Badge informativo y no bloqueante cuando el saldo carga OK; cola siempre visible/accionable.
- Test actualizado + regresión verde.
- CI **verde** en PR #16 (incluida integración real).
- Sin cambios de esquema (`migration list` sin cambios).
- **Versionar el propio `.md`** en `docs/prompts/FB-F3-22.md`.
- Cerrar con `dod-checklist`.
- Al terminar: `git status`. Sigue **re-auditoría acotada** (`FB-F3-AUD-22`) del punto tocado, antes del merge.
