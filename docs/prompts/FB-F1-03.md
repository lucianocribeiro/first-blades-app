# FB-F1-03 — Remediación Fase 1: hallazgos de FB-F1-AUD-01

- **ID:** FB-F1-03
- **Fecha:** 19 de junio de 2026
- **Destino:** Claude Code
- **Propósito:** Corregir los hallazgos bloqueantes y mayores de la auditoría FB-F1-AUD-01, ya adjudicados por Luciano.

## Contexto
Codex auditó FB-F1-01 + FB-F1-02 (FB-F1-AUD-01) y devolvió hallazgos. Luciano adjudicó. Este prompt corrige los confirmados. Algunos hallazgos se resolvieron corrigiendo el DoD y NO requieren cambio de código: están aclarados al final para que no los "arregles" de más.

## Hallazgos implementados

### (01) RLS Supervisor — restringir solo en documents y storage
- `supabase/migrations/0004_rls_fixes.sql`: DROP + recrear `documents_select` (solo propio para supervisor), `storage_documents_select` (idem).
- Supervisor mantiene SELECT de profiles de su equipo (Constitución §6, correcto).

### (02) Ocultar `estado` a no-admin
- `app/(app)/mi-perfil/ProfileView.tsx`: campo `estado` (StatusBadge) envuelto en `{isAdmin && ...}`.
- `entrevista_tecnica` y `estudio_medico` ya estaban gateados.

### (03 + 06) Admin sobre el perfil de un empleado — selector mínimo
- `app/(app)/mi-perfil/AdminEmployeeSelector.tsx`: búsqueda por nombre/apellido/email, dropdown, navega a `/admin/empleado/{id}`.
- `app/(app)/admin/empleado/[id]/page.tsx`: server component admin-only; carga perfil + documentos del empleado; ProfileView (isAdmin=true) + ProfileEditForm + DocumentsSection con `targetUserId`.
- `app/(app)/mi-perfil/AdminDocumentUploadModal.tsx`: todos los tipos incluyendo estudio_medico; llama `uploadDocumentForEmployee`; `estado = 'aprobado'` directo.
- `app/(app)/mi-perfil/actions.ts`: `searchEmployees(q)` + `uploadDocumentForEmployee(formData, employeeId)`.
- `app/(app)/mi-perfil/DocumentsSection.tsx`: prop `targetUserId?` — renderiza `AdminDocumentUploadModal` cuando está seteado.
- `app/(app)/mi-perfil/page.tsx`: agrega `<AdminEmployeeSelector />` para admin.

### (04) Storage INSERT — permitir al admin subir a carpeta de cualquier empleado
- `supabase/migrations/0004_rls_fixes.sql`: DROP + recrear `storage_documents_insert` con `OR (SELECT public.is_admin())`.

### (05) Gate de archivo purgado
- `app/(app)/mi-perfil/page.tsx`: `filter(!file_purged_at)` antes de generar signed URLs.
- `app/(app)/admin/empleado/[id]/page.tsx`: idem.
- `app/(app)/mi-perfil/DocumentsSection.tsx`: UI ya gateaba sobre `file_purged_at`.

### (08) Copy en lugar de strings hardcodeados
- `lib/copy/index.ts`: agrega `documentos.errors.*` (5 mensajes) y `adminEmpleado.*`.
- `app/(app)/mi-perfil/actions.ts`: reemplaza 5 strings hardcodeados.
- `app/(app)/mi-perfil/DocumentUploadModal.tsx`: reemplaza 1 string hardcodeado.
- `app/(app)/aprobaciones/actions.ts`: usa `copy.aprobaciones.rejectModal.motivoRequired`.

### (09) Tests de integración: skip limpio sin DB
- `tests/integration/global-setup.ts`: ping Postgres → `INTEGRATION_DB_AVAILABLE`.
- `vitest.integration.config.ts`: agrega `globalSetup`.
- Todos los archivos de integración: `const dbAvailable = process.env.INTEGRATION_DB_AVAILABLE === 'true'`, guard en `beforeAll`, `describe.skipIf(!dbAvailable)`.

## Fuera de alcance (no implementado)
- Notificación por mail al rechazar: pendiente §14 (Gmail address).
- Módulo Equipo completo (alertas de vencimiento, roster, listado masivo).
- Nulificar `storage_path` tras purga (es correcto no hacerlo).
