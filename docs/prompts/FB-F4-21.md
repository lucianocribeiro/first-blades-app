# FB-F4-21 — Bump de Constitución v0.6 → v0.7 (deltas de Fase 4)

> **Fase 4 · Cierre · Build (doc).** Para Claude Code, vía Luciano.
> Actualiza `docs/constitucion.md` de **v0.6** a **v0.7**, incorporando los deltas de Fase 4. La Constitución es la **fuente de verdad**: los deltas se redactan **contra el estado real verificado** (prod + repo), no de memoria, y la edición es **quirúrgica** — sólo lo que cambió, sin reescribir secciones estables.
> **App/doc-only, sin migración.** No es fix de feature → **no requiere auditoría de Codex formal**; alcanza CI verde + la revisión de Luciano del texto. (Igual pasa por PR y gate de merge.)

---

## 1. Objetivo

Dejar la Constitución al día antes de abrir Fase 5, reflejando el estado **final** de Fase 4 (ya sin la divergencia de audit, que se resolvió en 0018).

## 2. Inspección previa (contra el estado real — reportar)

Antes de editar, confirmar contra prod (MCP, solo-lectura) y el repo:
- **Migraciones en prod:** 0001–**0018**.
- **RPCs `SECURITY DEFINER`** existentes y sus atributos: `resolver_ausencia_request`, `resolver_pasaje_request`, `cancelar_editar_ausencia_aprobada`, `cancelar_editar_pasaje_aprobado`, más los helpers de rol.
- **Columnas/enums nuevos:** `ausencia_requests.motivo_otros_texto`; `pasaje_requests.dias_viaje`; `post_aprobacion_tipo` (enum) + `comentario_post_aprobacion` + `post_aprobacion_at` en ambas tablas; exclusion constraint `ausencia_requests_no_solapamiento_pendiente` (btree_gist).
- **CI:** el workflow ya tiene el job **E2E Playwright (stack efímero)**.

## 3. Deltas a incorporar en v0.7 (lista cerrada con Luciano)

Editar **sólo** las secciones afectadas; el resto queda igual.

**§2 / §2.3 (stack y migraciones):**
- CI ahora incluye **e2e Playwright** cableado como job (deuda §14.5 saldada). Actualizar §14.5 en consecuencia (ya no es deuda pendiente; queda la nota de que marcar el check como requerido es acción de settings de Luciano, y el ítem de estabilizar performance del runner está en el Log).

**§5 (modelo de datos):**
- `ausencia_requests`: agregar `motivo_otros_texto`; documentar la **exclusion constraint de no-solapamiento** de pendientes (btree_gist), que **reemplazó** el índice de duplicados exactos — no-solapamiento **sólo entre pendientes**, cualquier motivo.
- `pasaje_requests`: agregar `dias_viaje date[]` (días discretos) + su CHECK no-vacío.
- Ambas tablas: agregar `post_aprobacion_tipo` (enum `editada`/`cancelada`, nullable), `comentario_post_aprobacion`, `post_aprobacion_at`.

**§6.1 (RPCs `SECURITY DEFINER`):**
- Documentar las RPCs nuevas: `resolver_pasaje_request` (aprobar pasaje → `en_viaje` per-día en el `empleado_id`), `cancelar_editar_ausencia_aprobada` / `cancelar_editar_pasaje_aprobado` (cambio post-aprobación con **guarda LIFO**).
- Anotar que `resolver_ausencia_request` propaga el motivo per-día y que **todo el `audit_log` de calendario es por-día** (ausencia alineada a pasaje en 0018) — ya sin divergencia.

**Reglas de negocio (donde corresponda — §7 u otra):**
- No-solapamiento sólo entre pendientes; contra aprobadas se permite con **alerta de sobrescritura** (no bloqueante).
- **No-retroactiva** (server-side) en ausencias y pasajes.
- **Edición post-aprobación admin-directo:** comentario obligatorio, cancelar→días sin asignar, editar=borrar viejos+escribir nuevos, **guarda LIFO** (stack por `reviewed_at`, cruza tipos, solapamiento parcial bloquea), visibilidad in-app del cambio para el empleado.

**Patrón nuevo (§ patrones / §5 de código o donde encaje):**
- **Contrato return-based de Server Actions:** los `throw new Error(mensajeAmigable)` que cruzan el límite de una Server Action se **redactan en producción** (Next.js) → las actions devuelven `{ ok, error }` y los call sites chequean `!ok`. Codificarlo como regla para que no se reintroduzca.

**§ Skills / §14:**
- Actualizar §14.5 (e2e ya cableado). Revisar §14 para reflejar lo cerrado en Fase 4 (mantener sólo los pendientes reales).

## 4. Alcance y forma

- Editar `docs/constitucion.md`: encabezado a **v0.7** con una nota corta de "v0.7 incorpora los deltas de Fase 4: …" (breve, como la nota de v0.6).
- **Edición quirúrgica:** no reescribir secciones estables; tocar sólo lo listado. Mantener el estilo y el es-AR.
- Agregar al bloque de **"Decisiones cerradas"** una entrada v0.7 con la fecha y el resumen de deltas.
- Confirmar que `CLAUDE.md` sigue apuntando correctamente a `docs/constitucion.md` (sin cambios si ya apunta bien).
- **No** tocar código, migraciones, ni tests (salvo que algún test referencie literalmente la versión de la Constitución, en cuyo caso actualizar el string).

## 5. Pasos operativos

1. Rama nueva desde `main`.
2. Inspección (§2) → edición quirúrgica de `docs/constitucion.md` → bump a v0.7.
3. `commit → push → GitHub Actions`. CI verde (3 jobs; el e2e no debería verse afectado por un cambio de doc).
4. **Versionar** `docs/prompts/FB-F4-21.md`.
5. Reportar: **diff de las secciones tocadas** (para que Luciano revise el texto), confirmación de que sólo se editaron las secciones listadas, estado de CI, `git status`.

**No mergear.** Luciano **revisa el texto del diff** y gatea el merge. Sin auditoría formal (doc), sin runbook (no hay migración).

---

## 6. Definition of Done

- [ ] Inspección contra prod/repo reportada (migraciones 0001–0018, RPCs, columnas/enums, e2e en CI).
- [ ] `docs/constitucion.md` en **v0.7** con nota de deltas; edición quirúrgica (sólo las secciones listadas).
- [ ] §5 (columnas/enums/exclusion constraint), §6.1 (RPCs nuevas + audit por-día), reglas de negocio (no-solapamiento pendientes, no-retroactiva, edición post-aprobación + LIFO), patrón return-based, §14.5 (e2e cableado) — todos reflejados.
- [ ] Entrada v0.7 en "Decisiones cerradas" con fecha.
- [ ] `CLAUDE.md` apunta bien (sin cambios o corregido).
- [ ] CI verde (3 jobs); `docs/prompts/FB-F4-21.md` versionado; `git status` limpio.
- [ ] Diff de las secciones tocadas reportado para revisión de Luciano.
