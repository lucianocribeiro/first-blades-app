# FB-F4-04 — Fix: re-aseverar grants en 0015 + test de atomicidad bajo `asUser`

> **Fase 4 · Pieza 1 · Fix.** Para Claude Code, vía Luciano.
> Resuelve los dos hallazgos (Bajo) de `FB-F4-AUD-02`. **Config-hardening + solo-tests → NO requiere re-auditoría formal**: alcanza CI verde + drift detector verde + confirmar el test bajo `asUser`. Se commitea **sobre la misma branch `feat/fb-f4-03-resolver-rango` (PR #21)**, no un PR nuevo.

---

## 1. Objetivo

1. **Autosuficiencia de la migración 0015** (objeto `SECURITY DEFINER` sensible): re-aseverar explícitamente los `REVOKE`/`GRANT` tras el `CREATE OR REPLACE`, para no depender de que el ACL se preserve implícitamente.
2. **Cobertura del test de atomicidad de rango:** la invocación afirmada de `resolver_ausencia_request` debe correr bajo el **rol acotado** (`asUser` con claims de admin, rol `authenticated`), no bajo `asAdminSuperuser`/`postgres`.

---

## 2. Alcance

`supabase/migrations/0015_resolver_ausencia_rango.sql` (agregar 3 sentencias al final) y `tests/integration/resolver-ausencia-request.test.ts` (caso de atomicidad). **No** tocar el cuerpo de la función, la columna, ni ningún otro código.

### Fix 1 — grants explícitos en 0015
Agregar al **final** de `0015`, **copiando verbatim de `0013`** las mismas tres sentencias (para garantizar match exacto de firma y roles):
```sql
REVOKE ALL ON FUNCTION resolver_ausencia_request(...) FROM PUBLIC;
REVOKE ALL ON FUNCTION resolver_ausencia_request(...) FROM anon;
GRANT EXECUTE ON FUNCTION resolver_ausencia_request(...) TO authenticated;
```
- **Copiar la firma exacta** desde `0013` (mismos tipos de parámetros) para que el `REVOKE`/`GRANT` apunte a la función correcta y sea idempotente.
- Son idempotentes y producen el mismo end-state que ya verifica el drift detector; el objetivo es dejar el estado deseado explícito en la propia migración.

### Fix 2 — test de atomicidad bajo `asUser`
- Mantener el **setup DDL privilegiado** si hace falta para montar el fallo (ej. crear un CHECK temad de rango), **comentado** como setup indispensable.
- La **invocación afirmada** de `resolver_ausencia_request` debe correr bajo `asUser(IDS.admin)` (rol `authenticated` + claims de admin), de modo que el camino de ejecución sea el del rol acotado real. Afirmar que, ante el fallo a mitad de rango, **ninguna** fila del calendario persiste y la request **no** cambia de estado (rollback total).
- Alternativa aceptable: estructurar el fallo como fixture y hacer la llamada vía `asUser(IDS.admin)`; el criterio es que el paso afirmado no corra como `postgres`.

---

## 3. Tests

- El caso de atomicidad afirma la invocación bajo `asUser`; el privilegio, si se usa, es sólo setup (comentado).
- **Drift detector** sigue en verde: grants efectivos (`authenticated=true`, `anon=false`, `public=false`), `prosecdef`, `proconfig`, owner, firma — sin cambios respecto de lo ya auditado (las 3 sentencias no alteran el end-state, sólo lo explicitan).
- Suite completa verde en CI: integración RLS real (corrida de verdkip) + typecheck / lint / build.

---

## 4. Pasos operativos (cerrar cada uno)

1. Sobre la branch `feat/fb-f4-03-resolver-rango` (PR #21), aplicar los dos fixes.
2. **Versionar el informe de Codex verbatim** en `docs/audits/FB-F4-AUD-02.md` (el `.md` real, sin editar) — requisito previo al merge.
3. `commit → push → GitHub Actions`. Confirmar PR #21 vuelve a verde.
4. Reportar: el diff de las 3 sentencias de grants (verbatim), el test de atomicidad ahora bajo `asUser` (qué rol invoca), drift detector verde, estado de CI, y `git status`.

**No mergear ni pushear a la base.** El merge lo gatea Luciano (`FB-F4-MERGE-21`); el `db push` es `FB-F4-RUN-02`, aparte, con verificación de catálogo de función.

---

## 5. Definition of Done

- [ ] 0015 termina con los 3 `REVOKE`/`GRANT` verbatim de 0013 (firma exacta, idempotentes).
- [ ] Test de atomicidad afirma la invocación bajo `asUser`; privilegio sólo para setup (comentado).
- [ ] Drift detector verde (grants efectivos y atributos de la función sin cambios respecto de lo auditado).
- [ ] Integración corrida de verdad en CI (no skip); suite completa verde.
- [ ] Informe de Codex `FB-F4-AUD-02.md` versionado verbatim en `docs/audits/`.
- [ ] `docs/prompts/FB-F4-04.md` versionado.
- [ ] `git status` reportado; sin cambios sin commitear.
- [ ] PR #21 en CLEAN, listo para que Luciano gatee el merge.
